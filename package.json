{
  "name": "rails-json-api-react",
  "engines": {
    "node": "6"
  },
  "scripts": {
     "postinstall": "cd client && npm install && npm run build && echo 'Client built!'"
  },
  "cacheDirectories": [
    "client/node_modules"
  ]
}
