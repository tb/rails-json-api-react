{
  "name": "rails-json-api-react",
  "engines": {
    "node": "6"
  },
  "scripts": {
     "postinstall": "cd client && yarn && yarn run build"
  },
  "cacheDirectories": [
    "client/node_modules"
  ]
}
